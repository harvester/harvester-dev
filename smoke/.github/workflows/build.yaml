name: Build

on:
  push:
    branches:
      - main
  pull_request:
  release:
    types: [published]

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Determine version
        id: version
        run: |
          if [ "${{ github.event_name }}" = "release" ]; then
            VERSION="${{ github.event.release.tag_name }}"
          else
            VERSION=$(git rev-parse --short HEAD)
          fi
          echo "version=${VERSION}" >> $GITHUB_OUTPUT
          echo "Building version: ${VERSION}"

      - name: Build
        env:
          VERSION: ${{ steps.version.outputs.version }}
        run: make

      - name: Create release tarball
        run: |
          mkdir -p harvester-smoke
          cp bin/* harvester-smoke/ 
          tar -czf harvester-smoke-${{ steps.version.outputs.version }}.tar.gz harvester-smoke
          cd ..

      - name: Upload release asset
        if: github.event_name == 'release'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh release upload ${{ github.event.release.tag_name }} \
            harvester-smoke-${{ steps.version.outputs.version }}.tar.gz

      - name: Upload build artifact
        if: github.event_name != 'release'
        uses: actions/upload-artifact@v4
        with:
          name: harvester-smoke
          path: harvester-smoke-${{ steps.version.outputs.version }}.tar.gz
